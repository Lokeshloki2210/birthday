import { motion } from "framer-motion"; function MemoryModal({ memory, closeModal }) { return ( <div className="modal-overlay" onClick={closeModal} > <motion.div className="memory-modal" initial={{ scale: 0.5, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} transition={{ duration: 0.5 }} onClick={(e) => e.stopPropagation() } > <img src={memory.image} alt={memory.title} /> <h2> {memory.icon} {" "} {memory.title} </h2> <p> {memory.message} </p> <button onClick={closeModal} > Close ❤️ </button> </motion.div> </div> ); } export default MemoryModal;