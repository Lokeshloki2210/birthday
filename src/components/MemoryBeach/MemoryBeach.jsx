import { useState } from "react"; import { motion } from "framer-motion"; import MemoryCard from "./MemoryCard"; import MemoryModal from "./MemoryModal"; import "./memory.css"; const memories = [ { id: 1, icon: "🐚", title: "Golden Shell", image: "/images/swathi-memory-1.jpg", message: "Every beautiful memory with you is a treasure worth keeping forever." }, { id: 2, icon: "⭐", title: "Starfish Wish", image: "/images/swathi-memory-2.jpg", message: "You light up every place you go, just like stars brighten the night sky." }, { id: 3, icon: "🧸", title: "Little Teddy", image: "/images/swathi-memory-3.jpg", message: "Thank you for all the smiles, laughter, and wonderful moments we've shared." }, { id:4, icon: "🎂", title: "Birthday Cake", image: "/images/swathi-main.jpg", message: "Today belongs to you—celebrate, laugh, and create memories that last forever." } ]; function MemoryBeach() { const [selectedMemory, setSelectedMemory] = useState(null); return ( <section className="memory-section" id="memories" > <motion.div className="memory-header" initial={{ opacity: 0, y: 50 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 1 }} viewport={{ once: true }} > <h2> Treasure Shore 🐚 </h2> <p> Click the shells and tiny treasures to reveal birthday wishes for Swathi Ellikutty. </p> </motion.div> <div className="memory-grid"> {memories.map((memory) => ( <MemoryCard key={memory.id} memory={memory} onClick={() => setSelectedMemory(memory) } /> ))} </div> {selectedMemory && ( <MemoryModal memory={selectedMemory} closeModal={() => setSelectedMemory(null) } /> )} </section> ); } export default MemoryBeach;