import { useFrame } from "@react-three/fiber"; import { useRef } from "react"; function Ocean() { const oceanRef = useRef(); useFrame((state) => { oceanRef.current.rotation.z = Math.sin(state.clock.elapsedTime * 0.1) * 0.02; }); return ( <mesh ref={oceanRef} rotation={[-Math.PI / 2, 0, 0]} position={[0, -1, -5]} > <planeGeometry args={[50, 50, 128, 128]} /> <meshStandardMaterial color="#3da9fc" transparent opacity={0.9} /> </mesh> ); } export default Ocean;