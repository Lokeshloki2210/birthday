import { Float } from "@react-three/drei"; function FloatingParticles() { const particles = []; for (let i = 0; i < 40; i++) { particles.push( <Float key={i} speed={2} rotationIntensity={2} floatIntensity={3} > <mesh position={[ (Math.random() - 0.5) * 20, Math.random() * 8, (Math.random() - 0.5) * 20 ]} > <sphereGeometry args={[0.05]} /> <meshStandardMaterial color="#ffffff" emissive="#ffd9b3" emissiveIntensity={3} /> </mesh> </Float> ); } return particles; } export default FloatingParticles;